# Python virtual environment (the local "toolbox" — recreate with requirements.txt)
venv/

# Downloaded AI model weights (fetched with get_model.py, too large to store in git)
*.pt

# Python cache files
__pycache__/
*.pyc

# Machine-specific runtime data (drawn on your camera / measured on your line)
zones.json
cycles.csv

# The logbook database and its exports (your recorded data - keep it local)
*.db
logbook_export.xlsx
*.xlsx
